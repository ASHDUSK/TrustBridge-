# TrustBridge / MedBridge Studio 依赖（Windows/Linux，Python 3.13 实测）
# PyTorch 需从官方索引安装 CUDA 版（本仓库实测 2.14.0+cu126）：
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# 然后再安装以下依赖：
lightning==2.6.6
omegaconf==2.3.0
jsonargparse[signatures]>=4.27.7
scikit-image==0.25.2
nibabel==5.3.2
gradio==6.27.0
matplotlib==3.10.5
tqdm==4.67.1
PyYAML==6.0.3
rich==14.1.0
scipy>=1.15
numpy>=2.2
Pillow>=11
